# API App Placeholder

This folder reserves the future standalone API service boundary described in the technical blueprint.

Milestone 1 uses Next.js route handlers under `web/src/app/api/v1` as a minimal vertical slice while preserving migration safety.

No standalone API runtime is introduced yet.
